# Security policy

脚本以 root 安装软件和服务；运行时 sing-box 使用独立不可登录账号。
服务端支持 Ubuntu 24.04、sing-box 1.14.0 和 README 声明的固定配置。
Mac 客户端使用 README 声明的当前 Homebrew 稳定依赖，由 Homebrew 校验软件包。

不要在 Git、公开 issue、CI 日志中提交真实 PSK、私钥、客户端配置或原始真实主机报告。
报告问题时提供版本、最小合成复现与脱敏错误。一次性 VM 可以生成临时随机密钥，
密钥只保存在 guest 私有目录，不进入发布日志或制品。

怀疑凭据泄露时，经已验证的私密管理连接更换服务端和客户端密钥；本脚本的重复执行会
保留原密钥，不提供自动轮换。安装失败可能保留已完成步骤，应先检查错误和备份。
